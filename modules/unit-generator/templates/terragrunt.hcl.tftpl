# Generated by ${generated_by_module}

include "root" {
  path = find_in_parent_folders("root.hcl")
}

terraform {
  source = "."
}

%{ if length(dependencies) > 0 ~}
dependencies {
  paths = ${jsonencode([for item in dependencies : item.config_path])}
}
%{ endif ~}
