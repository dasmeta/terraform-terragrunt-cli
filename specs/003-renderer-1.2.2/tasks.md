# Tasks

- [x] Bump the `infra-yaml-loader` pin to 1.2.2.
- [x] Raise `required_version` to `~> 1.8` for the root module and `modules/unit-generator`.
- [x] Leave `modules/root-generator` at `~> 1.3` — no provider functions.
- [x] Update README requirement and module tables.
- [x] Reproduce the `linked-stacks` check failure on the 1.2.1 pin to prove it predates the bump.
- [x] Correct the over-escaped dependency expectation in the `linked-stacks` example.
- [x] Correct the JSON-shaped input expectation in the `linked-stacks` example.
- [x] Plan every example and confirm no failing checks.
- [x] Add `AGENTS.md` diagnostic guide.
- [ ] Publish a new driver version after merge to `main`.
